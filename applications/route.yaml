#app.kubernetes.io/instance: applications
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  annotations:
    argocd.argoproj.io/sync-options: Delete=false,ServerSideApply=true
  labels:
    app.kubernetes.io/instance: applications
  name: https-example-r9zcc
  namespace: ingress-test
spec:
  httpHeaders:
    actions:
      response:
      - name: X-Frame-Options
        action:
          type: Set
          set:
            value: DENY